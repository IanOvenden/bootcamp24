import styled, { css } from 'styled-components';

const StyledIxdSparkLink = styled.div(() => {
  return css`
    .ixd-spark-image {
      width: 100%;
      height: 150px;
      object-fit: cover;
    }
  `;
});

export default StyledIxdSparkLink;
